[
  { "command": "select * from role_user where role_id = 3 \\G", "description": "role_idが3のユーザーを縦表示で取得" },
  { "command": "select * from users where id = 1", "description": "IDが1のユーザーを取得" },
  { "command": "select count(*) from users", "description": "ユーザー数をカウント" },
  { "command": "select * from users order by created_at desc", "description": "作成日時の降順でユーザーを取得" },
  { "command": "select * from users limit 10", "description": "最初の10件を取得" },
  { "command": "select * from users where email like '%@example.com'", "description": "メールアドレスで部分一致検索" },
  { "command": "insert into users (name, email) values ('test', 'test@example.com')", "description": "新規ユーザーを挿入" },
  { "command": "update users set name = 'updated' where id = 1", "description": "ユーザー名を更新" },
  { "command": "delete from users where id = 1", "description": "ユーザーを削除" },
  { "command": "select u.name, r.name from users u join roles r on u.role_id = r.id", "description": "ユーザーとロールを結合して取得" },
  { "command": "select * from users where created_at >= now() - interval 1 day", "description": "直近1日以内に作成されたユーザーを取得" },
  { "command": "select role_id, count(*) from users group by role_id", "description": "ロールごとのユーザー数を集計" },
  { "command": "select * from users where status in ('active', 'pending')", "description": "ステータスが複数条件に一致するユーザーを取得" },
  { "command": "alter table users add column phone varchar(20)", "description": "テーブルにカラムを追加" },
  { "command": "create index idx_email on users(email)", "description": "インデックスを作成" },
  { "command": "show tables", "description": "テーブル一覧を表示" },
  { "command": "describe users", "description": "テーブル構造を確認" },
  { "command": "explain select * from users where id = 1", "description": "クエリの実行計画を確認" },
  { "command": "select distinct role_id from users", "description": "重複を除いてロールIDを取得" },
  { "command": "select * from users where name is not null", "description": "名前がNULLでないユーザーを取得" },
  { "command": "truncate table logs", "description": "テーブルのデータを全削除" },
  { "command": "select max(id) from users", "description": "最大IDを取得" },
  { "command": "select avg(age) from users", "description": "年齢の平均を計算" },
  { "command": "select * from users having count(*) > 1", "description": "件数が1より多いレコードを取得" },
  { "command": "begin transaction", "description": "トランザクションを開始" },
  { "command": "commit", "description": "トランザクションをコミット" },
  { "command": "rollback", "description": "トランザクションをロールバック" },
  { "command": "select * from users left join posts on users.id = posts.user_id", "description": "左外部結合でユーザーと投稿を取得" },
  { "command": "select * from users union select * from admins", "description": "2つのテーブルを結合" },
  { "command": "select coalesce(name, 'unknown') from users", "description": "NULLの場合にデフォルト値を返す" }
]

